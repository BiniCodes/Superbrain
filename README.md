## install dev backend server global

`npm install -g json-server`

## start mock server with

`json-server --watch --port 4000 --host <your ip adress> backend/db.json`

more infos :
https://github.com/typicode/json-server
